import {ForwardedRef, ReactNode, forwardRef, useRef} from 'react';
import {
  Dimensions,
  Pressable,
  Text,
  TextInput,
  TextInputProps,
  View,
} from 'react-native';

export const InputField = ({}) => {};
